---
# jenkins installed plugins
jexp_cli_hostname: 'localhost'
jexp_cli_jar_location: '/var/cache/jenkins/war/WEB-INF/jenkins-cli.jar'
jexp_http_port: 8080
jexp_application_context: ''
jexp_jenkins_system_user: 'jenkins'

jexp_installed_plugins_file: "{{ playbook_dir }}/group_vars/xx_installed_plugins.yml"


# configs

jexp_home_folder: "{{ playbook_dif }}/servers/xxxxx"


